name: PR title
on:
  pull_request:
    branches: [probes]
    types: [opened, reopened, synchronize, edited]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  title:
    name: Conventional PR title
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: echo "Sandbox trigger probe; actual title validation stays in generated main."
